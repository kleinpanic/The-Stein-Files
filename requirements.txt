requests==2.32.5
pypdf==6.6.2
pdfminer.six==20260107
jsonschema==4.26.0
Markdown==3.10.1
pytest==9.0.2
ruff==0.14.14

# OCR dependencies (Phase 2)
pytesseract==0.3.10
pdf2image==1.16.3
python-magic==0.4.27
Pillow==12.1.0

# NLP dependencies (Phase 3)
# (optional) spacy is intentionally not installed by default because it is large
# and not currently required for core extraction/build/test.
pdf2image>=1.17.0
